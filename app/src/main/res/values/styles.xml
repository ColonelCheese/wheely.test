<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="WheelyTheme" parent="AppTheme">
        <item name="actionBarStyle">@style/WActionBarStyle</item>
        <item name="android:actionBarStyle">@style/WActionBarStyle</item>
    </style>

    <style name="WActionBarStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name="displayOptions">showTitle</item>
        <item name="android:displayOptions">showTitle</item>
        <item name="background">@drawable/abc_ab_bottom_solid_light_holo</item>
        <item name="android:background">@drawable/abc_ab_bottom_solid_light_holo</item>
    </style>

</resources>
